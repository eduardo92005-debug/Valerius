{% form_theme form 'bootstrap_5_layout.html.twig' %}
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-outline-dark btn-lg w-100">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
