{% extends 'base.html.twig' %}

{% block title %}Edit Profile{% endblock %}

{% block banner_status %}
display:none;
{% endblock %}

{% block body %}
    {% if app.user.id == app.request.get('id') %}
        <h1>Edit Profile</h1>
        <div class="container">
            {{ include('UserAdmin/profile/_form.html.twig', {'button_label': 'Update'}) }}
            {{ include('UserAdmin/profile/_delete_form.html.twig') }}
        </div>
    {% else %}
        <h1 class="m-5">You are not authorized to view this page</h1>
    {% endif %}
{% endblock %}

