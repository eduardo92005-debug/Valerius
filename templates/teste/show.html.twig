{% extends 'base.html.twig' %}

{% block title %}Profile{% endblock %}

{% block body %}
    <h1>Profile</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ profile.id }}</td>
            </tr>
            <tr>
                <th>Full_name</th>
                <td>{{ profile.fullName }}</td>
            </tr>
            <tr>
                <th>Genre</th>
                <td>{{ profile.genre }}</td>
            </tr>
            <tr>
                <th>Birth_date</th>
                <td>{{ profile.birthDate ? profile.birthDate|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_teste_index') }}">back to list</a>

    <a href="{{ path('app_teste_edit', {'id': profile.id}) }}">edit</a>

    {{ include('teste/_delete_form.html.twig') }}
{% endblock %}
